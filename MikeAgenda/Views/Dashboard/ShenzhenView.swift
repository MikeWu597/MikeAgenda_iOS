import SwiftUI

struct ShenzhenView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("深圳")
                .font(.largeTitle)
                .foregroundColor(.secondary)
            Spacer()
        }
        .navigationTitle("深圳")
    }
}
